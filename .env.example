# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
DATABASE_URL="postgresql://postgres:user@localhost:5432/habitspark?schema=public"

# Authentication
# Use a long, random string for secrets in production
JWT_SECRET=your_jwt_access_secret_here
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_SECRET=your_jwt_refresh_secret_here
REFRESH_TOKEN_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

# CORS Configuration
# Set to your frontend URL in production
FRONTEND_URL=http://localhost:3000
